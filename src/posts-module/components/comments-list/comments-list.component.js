import template from './comments-list.template.html';
import controller from './comments-list.controller';

export default {
    template,
    controller,
    bindings: {
        comments: '='
    }
}